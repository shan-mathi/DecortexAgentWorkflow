{
  "name": "@agent-engine/workflow-engine",
  "version": "1.0.0",
  "private": true,
  "description": "Fargate service: Workflow Service + Executor Service. Owns all DAG logic, node execution, and Postgres.",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@aws-sdk/client-bedrock-runtime": "^3.700.0",
    "drizzle-orm": "^0.36.4",
    "expr-eval": "^2.0.2",
    "fastify": "^5.1.0",
    "@fastify/cors": "^10.0.1",
    "pg": "^8.13.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/pg": "^8.11.10",
    "tsx": "^4.19.2",
    "vitest": "^2.0.5"
  }
}
