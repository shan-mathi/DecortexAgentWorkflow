{
  "name": "@agent-engine/backend-api",
  "version": "1.0.0",
  "private": true,
  "description": "Lambda + API Gateway: thin proxy that validates UI requests and forwards to Workflow Engine (Fargate).",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/local.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@fastify/aws-lambda": "^4.1.0",
    "@fastify/cors": "^10.0.1",
    "fastify": "^5.1.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "vitest": "^2.0.5",
    "@types/aws-lambda": "^8.10.145"
  }
}
