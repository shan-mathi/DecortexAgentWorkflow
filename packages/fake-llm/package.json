{
  "name": "@workflow-engine/fake-llm",
  "version": "0.0.0",
  "private": true,
  "description": "Deterministic, deployable LLMProvider with controllable latency and failure modes.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@workflow-engine/shared": "workspace:*"
  }
}
