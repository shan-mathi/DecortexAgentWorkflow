{
  "name": "@workflow-engine/storage",
  "version": "0.0.0",
  "private": true,
  "description": "Postgres-backed WorkflowRepo and RunRepo (Drizzle + pg + pgvector).",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@workflow-engine/engine": "workspace:*",
    "@workflow-engine/shared": "workspace:*",
    "drizzle-orm": "^0.36.4",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@testcontainers/postgresql": "^10.13.2",
    "@types/pg": "^8.11.10",
    "@workflow-engine/fake-llm": "workspace:*",
    "testcontainers": "^10.13.2"
  }
}
