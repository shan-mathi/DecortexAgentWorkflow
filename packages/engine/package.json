{
  "name": "@workflow-engine/engine",
  "version": "0.0.0",
  "private": true,
  "description": "Pure DAG executor: NodeRegistry, NodeExecutor interface, runWorkflow.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./testing": "./src/testing/index.ts",
    "./testing/pluginContract": "./src/testing/pluginContract.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@workflow-engine/shared": "workspace:*",
    "expr-eval": "^2.0.2",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@workflow-engine/fake-llm": "workspace:*"
  }
}
