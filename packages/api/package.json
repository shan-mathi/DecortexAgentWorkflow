{
  "name": "@workflow-engine/api",
  "version": "0.0.0",
  "private": true,
  "description": "Fastify HTTP layer: workflow CRUD, run trigger, trace, /node-types.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/local.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.1",
    "@workflow-engine/engine": "workspace:*",
    "@workflow-engine/shared": "workspace:*",
    "fastify": "^5.1.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/pg": "^8.11.10",
    "@workflow-engine/fake-llm": "workspace:*",
    "@workflow-engine/storage": "workspace:*",
    "drizzle-orm": "^0.36.4",
    "pg": "^8.13.1",
    "tsx": "^4.19.2"
  }
}
