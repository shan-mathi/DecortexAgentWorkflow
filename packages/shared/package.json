{
  "name": "@workflow-engine/shared",
  "version": "0.0.0",
  "private": true,
  "description": "Shared types and Zod schemas for the agent workflow engine.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^4.4.3"
  }
}
